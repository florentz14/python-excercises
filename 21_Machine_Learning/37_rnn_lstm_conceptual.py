"""
RNN / LSTM - Conceptual Example
=================================
Recurrent Neural Networks for sequential data (text, time series, audio).
Full implementation typically uses TensorFlow or PyTorch, not scikit-learn.

Use cases: text sequences, time series, audio - when order matters.
Install: pip install tensorflow

Author: Florentino Baez
"""

# RNN/LSTM are designed for:
# - Text sequences
# - Time series
# - Audio
# - Any data where ORDER matters
#
# Common use: pip install tensorflow
# from tensorflow.keras.layers import LSTM
# from tensorflow.keras.models import Sequential
#
# model = Sequential()
# model.add(LSTM(64, input_shape=(timesteps, features)))
# model.add(Dense(1))
# model.compile(optimizer='adam', loss='mse')
# model.fit(X_train, y_train, epochs=10)

print("RNN/LSTM: Use TensorFlow or PyTorch for full implementation")
print("Ideal for: text, time series, audio - when order matters")
